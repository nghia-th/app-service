package vn.org.thn.app.base.persistence.query;

/** Sort direction for an {@code ORDER BY} clause built by the query DSL. */
public enum QueryDirection {
    /** Ascending order. */
    ASC,
    /** Descending order. */
    DESC
}
